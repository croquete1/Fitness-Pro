datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") // Pooler (6543)
  directUrl = env("DIRECT_URL")   // Direta (5432) para migrate/introspect
}

generator client {
  provider = "prisma-client-js"
}
