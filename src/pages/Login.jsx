import React from "react";
import LoginForm from "../components/LoginForm"; // verifica se o caminho está correto

const Login = () => {
  return (
    <div className="">
      <LoginForm />
    </div>
  );
};

export default Login;
